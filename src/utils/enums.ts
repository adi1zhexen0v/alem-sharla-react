export enum MessageTypes {
  TEXT = 'text',
  IMAGE = 'image',
  FILE = 'file',
}

export enum StatusTypes {
  NEW = 'new',
  PROGRESS = 'progress',
  COMPLETED = 'completed'
}

export enum StatusRusTypes {
  NEW = 'Новые',
  PROGRESS = 'В процессе',
  COMPLETED = 'Завершенные'
}